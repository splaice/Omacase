-- Techno Viking — Neovim colorscheme (read by lua/plugins/colorscheme.lua).
-- Unlike the other themes this is NOT a plugin: the colorscheme is the local
-- file ~/.config/nvim/colors/techno-viking.vim (shipped via
-- home/dot_config/nvim/colors/), which nvim finds on its runtimepath.
return "techno-viking"
