-- Extra options on top of LazyVim defaults (loaded before lazy starts).
-- LazyVim already sets a sane baseline; add omacase-specific tweaks here.
vim.opt.relativenumber = true
vim.opt.scrolloff = 8
