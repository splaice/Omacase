# omacase ranger config. The `omacase` colorscheme is a thin pass-through of
# ranger's default, whose ANSI colors track the active terminal theme via the
# palette. (It used to force magenta directories to match techno-viking, but that
# bled the accent into every theme — so it now just inherits the default.)
set colorscheme omacase
