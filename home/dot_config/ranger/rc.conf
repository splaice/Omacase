# omacase ranger config. Colors track the active terminal theme: the `omacase`
# colorscheme extends ranger's default but recolors directories to magenta
# (ANSI 5) so the file manager matches omacase's eza/ls. Because omacase swaps
# the terminal's ANSI palette per theme, ranger follows automatically.
set colorscheme omacase
