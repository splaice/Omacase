# omacase ranger config. The `omacase` colorscheme is a thin pass-through of
# ranger's default, whose ANSI file-list colors track the active terminal theme
# (Ghostty swaps the 16 ANSI colors per theme; greyscale themes render mono).
set colorscheme omacase
