# omacase ranger config. The `omacase` colorscheme recolors the file list from
# the active theme: `omacase theme` links that theme's color table to
# ~/.config/ranger/theme.colors, which the colorscheme reads. No table linked →
# ranger's stock default.
set colorscheme omacase
